
#
debug=true  
#
															
spring.datasource.url=jdbc:mysql://db.cercamister.it:3306/DB-ALLENATORE-0f5235ee-b830
spring.datasource.username=root
spring.datasource.password=Marcone76
server.port=8082
# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
#spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Implementazione Logger

logging.level.org.springframework.web=INFO
logging.level.it.peruvianit=DEBUG

#output to a temp_folder/file
#logging.file=${java.io.tmpdir}/application.log

#output to a file
logging.config=../../conf/errori/logback-spring.xml
rest.service.host=http://localhost:
rest.service.port=9092
rest.service.context=/api-in/territorio/
rest.service.regioni=regioni
rest.service.provincie=provincie/
rest.service.comuni=comuni/
spring.datasource.hikari.maximumPoolSize=2